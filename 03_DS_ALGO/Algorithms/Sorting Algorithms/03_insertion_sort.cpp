#include <iostream>
using namespace std;

int main()
{
    int array[] = {5, 7, 2, 1, 8, 9, 6, 2, 3, 1, 7, 0};

    int arraySize = (sizeof(array) / sizeof(array[0]));

    for (int i = 0; i < arraySize; i++)
    {
        int temp = array[i];
        


    }

    return 0;
}